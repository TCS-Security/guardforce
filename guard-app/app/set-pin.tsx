import { SetPin } from "@/ui/SetPin";
/** First PIN after claiming; the layout gate moves on when the stage flips to ready. */
export default function SetPinScreen() { return <SetPin onDone={() => undefined} />; }
